name: Dependency Scan

on:
  workflow_call:

jobs:
  dependency-scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          cd frontend
          npm audit --audit-level=high

      - run: |
          cd backend
          npm audit --audit-level=high
